[project]
name = "futurisys-ml-deployment"
version = "0.1.0"
description = "ML classification model deployment API with FastAPI"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.9",
]
